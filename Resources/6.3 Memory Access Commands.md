# ⚡ 快速記憶管理（Quick Memory Management）

現在我們來學習 **快速記憶管理**，它能幫助使用者在執行期間即時控制暫時性或單一 Session 的上下文。

你不需要去修改任何記憶檔案。

## 🏷 使用 `#`（井字號 / Pound）快速加入暫時記憶

新增臨時記憶最快、最方便的方法，就是使用 **井字號 `#`**。

它可以讓你快速調整當前 Session 的上下文，而不必直接編輯檔案。

### 📌 範例

你可以這樣輸入：

```markdown
# use camelCase for all new variables in this session
```

意思是：

> 在本次 Session 中，所有新變數請使用 camelCase 命名。

這個設定會在本次對話中持續生效，非常適合臨時調整。

## 🧪 實際操作示範說明

假設你有一個專案，其中有一個 `finance_tracker.py` 檔案，變數命名並沒有使用 CamelCase。

你會看到例如：

* `transaction_list`
* `validate_transaction`

接著你輸入：

```input
# use camelCase for the current Python file
```

Claude 便會：

* 自動把變數改為 camelCase
* 更新相關函式名稱
* 套用同一命名規範

例如：

* `transaction_list` → `transactionList`
* `validate_transaction` → `validateTransactionAmount`

---

## 🔁 記憶如何被延續？

當你開啟新的 Terminal 或重新進入 Claude Code：

* 系統會詢問是否要 **恢復上一個 Session**
* 你會看到之前輸入的 hash 指令：

  ```input
  # use camelCase for the current Python file
  ```

這代表 Claude 仍記得你剛才設定的暫時規則，並繼續套用。

---

## 🧭 使用 `/memory` 指令查看與操作記憶

另一個互動記憶的方式是使用：

```markdown
/memory
```

這個指令會顯示目前 Claude 所使用的記憶內容。

你可以看到：

* 專案記憶（Project Memory）
* 使用者記憶（User Memory）

你可以選擇查看其中一種，例如 Project Memory，即可看到該專案目前使用的記憶設定。

## 📂 `/memory` 的功能重點

* ✔ 查看目前載入的記憶內容
* ✔ 快速驗證 Claude 是否遵守專案規範
* ✔ 幫助除錯 AI 行為
* ✔ 提供治理與可審計性

---

## 🎯 總結

| 功能         | 說明                             |
| ------------ | -------------------------------- |
| `#` 指令     | 快速加入暫時記憶（僅限當前 Session） |
| `/memory`    | 查看目前載入的所有記憶            |
| Memory Files | 長期記憶（跨 Session 持久）       |
| Hash Memory  | 臨時上下文調整                    |

---

> 💡 快速記憶管理讓 Claude 能像真正的協作夥伴一樣，動態適應你的即時需求，同時保持長期一致性。

```bash
claude
    /init
    /auto-commit
    
    # use camelCase for all Python files in expense-tracker directory
    /memory
